{
  "name": "web3nity-students",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "import": "tsx scripts/import.ts",
    "import:dry": "tsx scripts/import.ts --dry-run",
    "test": "vitest run",
    "test:parser": "vitest run --dir packages/parser",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
